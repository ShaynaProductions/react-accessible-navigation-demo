import React from "react";
import { BaseProps } from "@/ui/types";

export interface DefinitionListProps extends BaseProps {
  children: React.ReactNode;
}

export interface DefinitionDetailPrpps extends BaseProps {
  children: React.ReactNode;
}

export interface DefinitionTermProps extends BaseProps {
  children: React.ReactNode;
}
